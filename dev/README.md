# fumux

take your audio and turn it into mono vorbis ogg. specifically that and nothing else.

ffmpeg under the hood, with WASM, so it all runs in your browser.

why can nobody make a good ffmpeg wrapper.


to run:
```bash
jspm serve -p nodemodules
```